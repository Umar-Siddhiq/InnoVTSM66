# Project Memory Index

Generated from `.ai/memory/events.jsonl`. Read critical items first, then search the event log for task-specific terms.

## Critical

- **CRITICAL / CORRECTION** `2026-09-03` - **Reverted Nuvoton M032 12-bit ADC scaling and restored M031 10-bit ADC scaling**: Reverted NUVOTON_M032 macro and 12-bit ADC (/4095.0f) scaling in custom_feature_def.h, MCU.h, and MCU.c. The 12-bit divisor caused 10-bit M031 ADC values (~500 at 12V mains) to be scaled down to 3.68V. Because 3.68V is below the 6.0V threshold in Hardware.c (PeriPheralVal.MainsVolt > 6.0), PeriPheralVal.IsMain was set to 0 (power disconnected) while ignition remained 1, causing  packet to show '...,1,0,3.6,3.5,...' and GETVSTATUS SMS to show 'MV: 3.68'. Restored standard 10-bit scaling (/1023.0f). Guard: Do not define NUVOTON_M032 or use 12-bit ADC scaling for M031 boards; always verify ADC bit depth and MainsVolt threshold (>6.0V) when touching voltage conversion.

## Recent

- **FIX** `2026-08-28` - **Nuvoton M032 ADC and Battery Telemetry Support**: Added NUVOTON_M032 conditional macro in custom_feature_def.h. Fixed 12-bit ADC voltage scaling and added battery voltage parsing.
- **MEMORY** `` - **Untitled**: AMD3 SET/GET/CLR OTA commands (per AIS-140 Amendment 3 Table 4D, $<IMEI>,<PASSWORD>,SET/GET/CLR,<cmd>:<val>*CS frame) sent via SMS or BLE were silently ignored - DecodeSMS() only understood the legacy ACTV/HCHK/+S*R textual commands and never checked for or routed the AMD3 frame, unlike TCP/RS232/RS485 which correctly route through DecodeOTAData()->ParseStandardAIS140Command().
- **MEMORY** `` - **Untitled**: OTA response SOURCE field (embedded in $PVT OTAResp, e.g. '(SCK1\|SET\|002:192.168.1.1:1)') sent a generic channel label (SCK1/SCK2/SMS/etc) for every source, but the AMD3 PDF's own worked examples require the actual originating address: server IP:port for a socket-received command, or the sender's phone number for SMS.
- **MEMORY** `` - **Untitled**: Fixed the coupled bug flagged in the prior session: (1) MakeParamChangeString() ignored its own Sender parameter via '(void)Sender;' and always emitted a generic channel label (SCK1/SCK2/etc) into the $PVT OTAResp SOURCE field for legacy 'SET:XXX'/'GET:XXX'/'CLR:XXX' style SMS commands, instead of the spec-required IP:port or phone number. (2) Its caller SendResponce() passed the wrong VTSData.ServerData index for OTA_SRC_SCK_2 (passed IP3 instead of IP2) and OTA_SRC_SCK_3 (passed IP4 instead of IP3), and had no case at all for OTA_SRC_SCK_4 - which fell through to the final else branch and wrongly called BLE_SendReply() for a genuine socket-4-sourced command.
- **CRITICAL / CORRECTION** `2026-09-03` - **Reverted Nuvoton M032 12-bit ADC scaling and restored M031 10-bit ADC scaling**: Reverted NUVOTON_M032 macro and 12-bit ADC (/4095.0f) scaling in custom_feature_def.h, MCU.h, and MCU.c. The 12-bit divisor caused 10-bit M031 ADC values (~500 at 12V mains) to be scaled down to 3.68V. Because 3.68V is below the 6.0V threshold in Hardware.c (PeriPheralVal.MainsVolt > 6.0), PeriPheralVal.IsMain was set to 0 (power disconnected) while ignition remained 1, causing  packet to show '...,1,0,3.6,3.5,...' and GETVSTATUS SMS to show 'MV: 3.68'. Restored standard 10-bit scaling (/1023.0f). Guard: Do not define NUVOTON_M032 or use 12-bit ADC scaling for M031 boards; always verify ADC bit depth and MainsVolt threshold (>6.0V) when touching voltage conversion.
- **FIX** `2026-09-03` - **Add SOS Tamper Disable alert to server by default with SMS/OTA commands**: Added DisableSOSTamper field to VTSTypedef with DEFAULT_DISABLE_SOS_TAMPER defined as 1 (alert to server disabled by default) in VTS.h. In File.c, LoadDefault sets VTSData.DisableSOSTamper = 1 and LoadConfig sanitizes it. In SOS.c, ProcessSOS checks !VTSData.DisableSOSTamper before raising SOS_TMP_ALERT or marking critical packet ready. In Server.c, CheckAlerts suppresses and clears SOS_TMP_ALERT if DisableSOSTamper is active. In SMS.c, added query and set commands: SET:SOSTMPDISABLE 1/0, SET:SOSTAMPERDISABLE 1/0, SET:SOSTAMPER 1/0, SET:SOSTMP 1/0, GET:SOSTMPDISABLE, and GET:SOSTAMPER. Guard: Do not remove DisableSOSTamper check from Server.c CheckAlerts or SOS.c ProcessSOS without verifying server alert requirement.
- **FIX** `2026-09-08` - **OG OA response routing, history replay and SOS fallback**: Confirmed OA replies were misrouted by comparing an IP source to SCK labels, and Source[8] truncated addresses. Added separate Channel and Source[64], restricted response consumption to OA/12, retained pending on failed sends. History replay deleted frames over 256 bytes and did not update XOR after H/SP conversion; now uses packet buffer capacity and recalculates XOR. OG emergency sends now run independently of primary connectivity, save failed EPB delivery and retry Mob0 SMS submission on SOS intervals until modem acceptance; button handling schedules server-thread delivery. Corrected earlier memory assumption: logical SCK_2 uses socket[2]/IP3 and SCK_3 uses socket[3]/IP4; socket[1]/IP2 is emergency. Guard: Keep routing identity separate from printable Source. Match actual inbound socket dispatch rather than numbered field assumptions. Test long PVT, H/SP XOR, independent EPB failure storage, later TCP failure and SMS retry. Preserve ADC/GNSS and SOS tamper suppression. Shared LIFO queue still waits behind primary-bound records; modem SMS acceptance is not handset receipt.
- **FIX** `2026-09-09` - **Fix GETVSTATUS all NC on wrong Server 2 port and emergency fallback**: Add ReinitSingleSocket to isolate server configuration changes, close old sockets to prevent M66 socket descriptor exhaustion, prevent false profile autoswitch, add EPB emergency fallback to Server 1, and add Mob1 SOS SMS fallback.
- **FIX** `2026-09-09` - **Fix DEFAULT_IP2 trailing comma, add EXTENDED_IPS socket 3 to PRF_AUTOSWITCH, and update VSTATUS NA display**: Fixed trailing comma in DEFAULT_IP2 for NIC_RAJASTHAN which caused DNS/IP conversion failure. Added ServerSocket[3] check to PRF_AUTOSWITCH in Systic.c to prevent premature profile switching when extended IP is active. Updated VSTATUS in SMS.c to report NA instead of NC when servers are configured as NA.
- **FIX** `2026-09-09` - **Graceful VRESET with OA,12 alert and System Restarting status**: Replaced immediate ThreadSleep(6000) and hard Ql_Reset(0) in VRESET handling with QueueVResetResponse. Now properly populates LastOTAResponse with Mode=SET, CmdId=VRESET, Value=System Restarting, Status=1, Pending=1, AddAlert(CONF_CHANGE_ALERT), and sets AIS140ResetPending=2. This allows the server loop to transmit the OA,12 alert frame (,...,OA,12,L,...(SRC\|SET\|VRESET:System Restarting:1)) and confirm delivery before executing SystemRecovery_RequestReset(VRESET). Guard: Never call Ql_Reset(0) immediately upon receiving OTA reset commands; use LastOTAResponse.Pending=1 and AIS140ResetPending=2 so the server loop flushes OA,12 frame first.
- **FIX** `2026-09-09` - **Store last fixed GPS data and fallback when unfixed**: Implemented FEATURE_GPS_LAST_FIX_FALLBACK in custom_feature_def.h, GPS.h, and GPS.c. When GPS is fixed, live values (Latitude, Longitude, Direction, Altitude, Speed, Heading, PDOP, HDOP, and string representations) are cached into LastFixedGPS. When GPS fix is lost, LastFixedGPS values are populated into active GPS fields and string buffers so packets to server and SMS status/location display the last known coordinates, while GPSFix accurately reports 0 (unfixed). When GPS re-acquires a fix, live data resumes immediately. Guard: Protected area **/*gps* authorized by user; GPS.GPSFix continues to report real fix status (0 or 1) in packets, while location/speed/alt/heading/PDOP/HDOP fields carry last known values when unfixed.
